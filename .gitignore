node_modules/
.DS_Store
*.log

# Config con API key / secret — non versionare.
# I file di config dei componenti vivono ora nei rispettivi submodule (services/*)
# e sono ignorati dai loro .gitignore. Qui si copre solo l'ambiente dell'umbrella.
.env
.env.dev
.env.prod

# Export JSON di staging/debug generati dall'API — non versionare
/museum.json
/steps.json

# Rendering generato dal PDF del design system (rigenerabile con shoot.mjs) — non versionare
docs/_design_pdf_pages/
